import { useState, useRef, useEffect } from "react";
import { useContext } from "react";
// import { anantaContext } from "./Home";

const VideoMeliora = () => {
    const videoRef = useRef(null);
    // const { click, end, setEnd } = useContext(anantaContext);
  
    // const togglePlay = () => {
    //   if (!isPlaying) {
    //     setIsPlaying(!isPlaying);
    //     videoRef.current.play();
    //   }
    // };
    
    return (
        <div>

        </div>
    )
}

export default VideoMeliora;